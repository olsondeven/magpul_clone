module.exports = {
  splitDollar: function(str){
    if(str.match(/\$/gi)){
      let strArr = str.split(/\$/gi);
      return splitStr;
    }else{
      let strArr = [];
      return (strArr.push(str));
    }
  }
};
